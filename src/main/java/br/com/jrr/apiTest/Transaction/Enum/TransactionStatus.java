package br.com.jrr.apiTest.Transaction.Enum;

public enum TransactionStatus {
    pending,
    approved,
    authorized,
    in_process,
    in_mediation,
    rejected,
    cancelled,
    refunded,
    charged_back;
}
